import { GetEventCoverImagePipe } from './get-event-cover-image.pipe';

describe('GetEventCoverImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetEventCoverImagePipe();
    expect(pipe).toBeTruthy();
  });
});
